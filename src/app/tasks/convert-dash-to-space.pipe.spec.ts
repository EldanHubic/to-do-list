import { ConvertDashToSpacePipe } from './convert-dash-to-space.pipe';

describe('ConvertDashToSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertDashToSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
